package me.lizardofoz.inventorio.slot

import net.minecraft.entity.player.PlayerInventory

class UtilityBeltSlot(inventory: PlayerInventory, index: Int, x: Int, y: Int) : ExtensionSlot(inventory, index, x, y)